import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abhaynaveen.insightdash',
  appName: 'insight-dash',
  webDir: 'dist'
};

export default config;
